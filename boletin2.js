document.addEventListener("DOMContentLoaded", () => {
  const archivo = "data/mujeres.txt";
  const maxSeleccion = 25;
  const siguiente = "boletin3.html";

  iniciarBoletin(archivo, maxSeleccion, siguiente, "boletin2");
});
