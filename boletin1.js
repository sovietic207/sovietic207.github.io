document.addEventListener("DOMContentLoaded", () => {
  const archivo = "data/hombres.txt";
  const maxSeleccion = 25;
  const siguiente = "boletin2.html";

  iniciarBoletin(archivo, maxSeleccion, siguiente, "boletin1");
});

